import 'package:flutter/material.dart';
import 'package:rswk_app/models/cek.dart';

class DftarPage extends StatefulWidget {
  const DftarPage({Key? key, this.cek}) : super(key: key);

  final Cek? cek;

  @override
  State<DftarPage> createState() => _DftarPageState();
}

class _DftarPageState extends State<DftarPage> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
